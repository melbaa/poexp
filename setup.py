from setuptools import setup

setup(
    name='melbalabs_poexp',
    packages=['melbalabs.poexp'],
    include_package_data=True,
    zip_safe=False,
    use_scm_version=True,
    setup_requires=['setuptools_scm'],

)
